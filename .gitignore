# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
dist/
*.egg-info/
.pytest_cache/
.mypy_cache/

# Virtual envs
venv/
env/
.venv/

# Data and logs
*.db
*.sqlite
*.log
data/
logs/

# Editor
.vscode/
.idea/
*.swp
*.swo
.DS_Store
Thumbs.db

# Secrets
.env
*.key
*.pem
config.local.*

# Claude Code per-machine / personal (do not commit)
CLAUDE.local.md
.claude/settings.local.json

# Per-Run hook IPC state (regenerated each run)
run_state/

# Scratch deliverables produced by CC during live integration test runs.
# These are throwaway files written into the repo root by scenarios that ask
# CC to "write a function" etc. Listed explicitly (not /*.py) so real root-level
# files like bootstrap.py stay tracked.
/hello.py
/process_transactions.py
/process_list.py
/square_fn.py
/fibonacci.py
/factorial.py
/calc2.py
